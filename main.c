#include <stdio.h>
#include <stdlib.h>

#include "parser.h"

int main(int argc, char **argv)
{
    if(argc < 2)
    {
        printf("Invalid use! Try: %s <file_name>", argv[0]);
        exit(1);
    }


}